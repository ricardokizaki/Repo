@model IEnumerable<WebApplication1.Models.AnimalModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Idade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Dono)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Idade)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = ((List<AnimalModel>)Model).IndexOf(item) }) |
                    @Html.ActionLink("Details", "Details", new { id = ((List<AnimalModel>)Model).IndexOf(item) }) |
                    @Html.ActionLink("Delete", "Delete", new { id = ((List<AnimalModel>)Model).IndexOf(item) })
                </td>
            </tr>
        }
    </tbody>
</table>
